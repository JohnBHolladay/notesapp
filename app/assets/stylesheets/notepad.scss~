// Place all the styles related to the notepad controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

body {
  background-color: #EEEEE;
}

.noteFull {
  padding: .5em;
  float: left;
  background-image: url(asset-path('notesapp_note.png'));
  background-repeat: no-repeat;
  width: 300px;
  height: 400px;
}

#newNote {
  background-image: url(asset-path('notesapp_notepad.png'));
}

textarea {
  background-color: transparent;
  width: 280px;
  height: 320px;
  margin-top: 40px;
  border: none;
  border-color: Transparent;
  outline: none;
  overflow: auto;
}

.noteText {
  resize: none;
  overflow: hidden;
}

.noteDeleteBtn {
  text-decoration: none;
  color: red;
  font-size: 12px;
}

#centerNotes {
  position: relative;
  margin: 0 auto;
  padding: 1em;
  padding-top: 2em;
  width: 100%;
}
